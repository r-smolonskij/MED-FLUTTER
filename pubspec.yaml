name: med
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  sqflite: ^1.1.7
  path_provider: ^1.3.0
  font_awesome_flutter: ^8.5.0
  expandable: ^3.0.1
  nice_button: ^0.1.7
  rflutter_alert: ^1.0.3
  flip_card: ^0.4.4
  provider:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - lib/assets/database.db
    - lib/assets/doctors.jpg
